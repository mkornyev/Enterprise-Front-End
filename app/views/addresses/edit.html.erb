<%= render "partials/header" %>

<div class="container">
  <div class="row no-gutters">
    <div class="col pl-3 pt-5">

      <!-- Edit Pane --> 
      <h4 class="lead">Editing Address:</h4><br>

      <fieldset class="border">
        <div class="ml-4 pl-2 my-4">
          <%= render 'form', address: @address %>
        </div>
      </fieldset>
      <%= link_to 'Take Me Back', addresses_path(@address) %>
    </div>
  </div>
</div>

<%= render "partials/footer" %>